kind: Service
apiVersion: v1
metadata:
  name: online-shop-service
  namespace: canary-ns
spec: 
  selector:
    app: online-shop-deployment
  ports:
    - protocol: TCP
      ports: 8080
      targetPort: 8080